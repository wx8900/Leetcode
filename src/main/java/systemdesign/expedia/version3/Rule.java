package systemdesign.expedia.version3;

/**
 * @ClassName Rule
 * @Description
 * @Author Jeff
 * @Date 2019/3/24 13:15
 * @Version V1.0
 */
public class Rule {

    /*private int determinWinner(ChessBoard chessBoard, Piece piece, Player currentPlayer) {
        int rowLength = chessBoard.getRowLength();
        int[] rows = chessBoard.getRows();
        int[] cols = chessBoard.getCols();
        int diagonal = chessBoard.getDiagonal();
        int antiDiagonal = chessBoard.getAntiDiagonal();
        int x = piece.getX();
        int y = piece.getY();

        int sign = currentPlayer.getId() == 1 ? 1 : -1;
        rows[x] += sign;
        cols[y] += sign;
        if (x == y) {
            diagonal += 1;
        }
        if (x == rowLength - 1 - y) {
            antiDiagonal += 1;
        }

        if (rowLength == ) {

        } else {
            if () {

            }
            return -100;
        }
    }*/
}
