package leetcode.microsoft;

public class AddNewCharTest {

    /*AddNewChar _addNewChar = new AddNewChar();

    @Test
    public void testGetString() {
        String str = "Seattle has lot snow.";
        char[] ch = new char[str.length()];
        for (int i = 0; i < str.length(); i++) {
            ch[i] = str.charAt(i);
        }

        String expected = new String("Seattle%20has%20lot%20snow.");
        String result = _addNewChar.URLify(ch);

        Assert.assertEquals(result, expected); // Alt + Enter : 导入包,自动修正 // Ctrl+Alt+O 优化导入的类和包
    }*/

}